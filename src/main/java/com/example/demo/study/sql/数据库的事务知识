数据库ACID
原子 一致 隔离 持久
一致性就是数据满足所有数据库的条件，比如字段约束，外键约束，触发器等。 数据库事务需要满足一致性，就是值一个事务从一致性开始，必须一致性结束。
一致性与数据库事务的其他三个特性不同，一致性是目的，其他三个是实现一致性的手段。
有的地方也把一致性理解为业务的一致性，比如转账，就必须满足转账前后总金额一致，但是这些是依赖业务的具体实现，并不由数据库事务本身来保证

脏读 不可重复读 幻读

脏读：事务B读取事务A的数据后 事务A回滚 这时候事务B读取到的数据就成为了脏数据
不可重复读：事务B读取事务A的数据后 事务A修改并提交事务 事务B再读取数据，此时数据改变了，即不可重复读
幻读：事务B读取事务A的数据后 事务A新增或删除了数据并提交 造成了量的变化 这时候事务B再读取就会造成幻读